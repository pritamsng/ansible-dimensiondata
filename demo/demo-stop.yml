---
  - name: Shutdown a DimensionData Host
    hosts: localhost
    vars:
      server_ids:
        - '9f5973cd-ad8b-4e2c-b1fa-d406290dc1f5'
    tasks:
      - name: Shutdown host
        dimensiondata:
          state: stopped
          server_ids: '{{ server_ids }}'
